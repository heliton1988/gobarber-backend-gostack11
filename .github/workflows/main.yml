name: CI

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      
      - name: Setup Node.js environment
        uses: actions/setup-node@v2.1.4
        with:
          node-version: 12.x

      # Install the dependencies npm/yarn
      - name: Install the dependecies
        run: yarn
        
      # Run the build
      - name: Run build
        run: yarn build
      
      # Copy the dist folder for into of Digital ocean
      - name: Copy dist folder to Digital Ocean
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          port: ${{ secrets.SSH_PORT }}
          key: ${{ secrets.SSH_KEY }}
          source: "."
          target: "~/app/node-deploy"
      
      # Replace the existing code on "dist" folder for the new code
      # Execute "yarn" on Digital Ocean
      # Execute the migrations of TypeORM
      # Restart server Node.js
      
